## Packages
framer-motion | Smooth animations for file upload and list transitions
date-fns | Formatting timestamps for document history
clsx | Class name utility
tailwind-merge | Class name utility

## Notes
- Upload endpoint expects `multipart/form-data` with a field named `file`.
- Status polling is required for checking OCR progress.
- No authentication required for MVP.
